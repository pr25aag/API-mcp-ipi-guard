fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
pydantic-settings>=2.2
httpx>=0.27
python-multipart>=0.0.9

# optional — only needed if LLM_PROVIDER=anthropic
anthropic>=0.34

# optional — only needed once GUARD_MODEL_PATH points at a real checkpoint
# torch>=2.2
# transformers>=4.41
